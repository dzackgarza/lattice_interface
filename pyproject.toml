[project]
name = "lattice-interface"
version = "0.0.0"
requires-python = ">=3.13"

[tool.uv]
package = false

[tool.uv.sources]
sage-stubs = { git = "https://github.com/dzackgarza/sage-stubs.git" }

[dependency-groups]
dev = [
    "sage-stubs",
]
